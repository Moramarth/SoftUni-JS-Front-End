function largestNumber(num1, num2, num3) {
    let output;
    output = Math.max(num1, num2, num3);

    // if (num1 > num2 && num1 > num3) {
    //     output = num1
    // } else if (num2 > num1 && num2 > num3) {
    //     output = num2
    // } else {
    //     output = num3
    // }
    console.log(`The largest number is ${output}.`);
}